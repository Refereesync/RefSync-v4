import { StyleSheet } from 'react-native';

export const watchStyles = StyleSheet.create({
  screen: { flex: 1, padding: 10, backgroundColor: '#000' },
  button: { margin: 10, padding: 20, backgroundColor: '#444' },
  text: { fontSize: 18, color: '#fff' }
});